[project]
name = "bdaykeeper-bot"
version = "0.1.0"
description = "Telegram-бот дней рождений на deep agents с human-in-the-loop"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.7,<0.8",
    "langchain>=1.0",
    "langchain-openai>=0.3",
    "langgraph>=1.1",
    "aiogram>=3",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
